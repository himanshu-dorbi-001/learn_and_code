{
  "name": "geolocateapp-ts",
  "version": "1.0.0",
  "description": "TypeScript console application for Google Geocoding API.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "console": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^20.11.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.6.0"
  }
}
